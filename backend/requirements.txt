# STORY-043: bumped from 0.115.6 -- pip-audit found 9 known vulnerabilities
# (PYSEC-2026-161/248/249/1941/1942/2280/2281) in the starlette version
# 0.115.6 pins in (<0.42.0). No version of starlette in that range is
# patched; 0.135.0 is the minimum FastAPI version that drops the upper
# bound entirely (starlette>=0.46.0, no cap), letting pip resolve the
# latest starlette (1.6.0 at audit time), which fully resolves all 9.
fastapi==0.135.0
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
redis==5.2.1
alembic==1.14.0
# STORY-051: the official Prometheus client -- zero transitive dependencies
# of its own. Hand-rolling the text-exposition format correctly (thread-
# safe counters, cumulative histogram buckets, proper HELP/TYPE headers)
# is genuinely fiddly; this is the one deliberate exception to this
# project's usual "no new dependency" discipline, justified because the
# Story's own literal FR names this exact format.
prometheus_client==0.26.0
