[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: requires a live Postgres and/or Redis (see tests/conftest.py)
    postgres: requires a live, isolated test Postgres database
    redis: requires a live, isolated test Redis DB index
